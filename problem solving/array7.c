#include<stdio.h>
#include<string.h>
int main()
{
    char str1[100],str2[100],strlen;
    int i,c=0;
    printf("enter the frist string");
    gets(str1);
    printf("enetr the second string");
    gets(str2);
    if( strlen[str1]== strlen[str2]);
    {
        for(i=0;str1[i]!='\0';i++)
        {
            if(str1[i]==str2[i]);
            {
                c++;
            }
        }
    }
}